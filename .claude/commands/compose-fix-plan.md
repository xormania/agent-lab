Read `AGENTS.md` and `CLAUDE.md` first.

Create a small, PR-sized plan to improve the Docker Compose layout for `agent-lab`.

Priorities:

1. Default-deny agent networking.
2. Localhost-only operator ports.
3. Explicit egress gateway path.
4. Clear profiles.
5. No privileged containers.
6. No Docker socket mounts.
7. No real secrets.
8. Accurate docs.

Do not implement yet.

Output:

- Current state summary.
- Proposed file changes.
- Boundary impact.
- Validation commands.
- Risks or assumptions.
