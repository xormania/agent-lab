# >>> GENERATED by tools/render-adapters.sh — DO NOT EDIT (edit policy/* and re-run) >>>
# Codex execpolicy: allow local git + forbid remote git/PR. The PreToolUse guard
# (tools/pretooluse-guard.sh) is the AUTHORITATIVE denier; these are belt-and-suspenders.
# Token-based: path-prefixes and origin/* refs are guard-enforced, not expressible here.
# CODEX_V1: 'git fetch' intentionally omitted — workspace-write sandbox is network-off.

# -- allow: local git (clean token prefixes only; workspace-write auto-runs the rest) --
prefix_rule(pattern = ["shellcheck"], decision = "allow", justification = "agent-lab lint")
prefix_rule(pattern = ["git", "add"], decision = "allow", justification = "agent-lab local-git set")
prefix_rule(pattern = ["git", "commit"], decision = "allow", justification = "agent-lab local-git set")
# CODEX_V1: ["git","fetch"] N/A — network-off
prefix_rule(pattern = ["git", "switch"], decision = "allow", justification = "agent-lab local-git set")
prefix_rule(pattern = ["git", "checkout"], decision = "allow", justification = "agent-lab local-git set")
prefix_rule(pattern = ["git", "branch"], decision = "allow", justification = "agent-lab local-git set")
prefix_rule(pattern = ["git", "stash"], decision = "allow", justification = "agent-lab local-git set")
prefix_rule(pattern = ["git", "restore"], decision = "allow", justification = "agent-lab local-git set")
prefix_rule(pattern = ["git", "status"], decision = "allow", justification = "agent-lab local-git set")
prefix_rule(pattern = ["git", "diff"], decision = "allow", justification = "agent-lab local-git set")
prefix_rule(pattern = ["git", "log"], decision = "allow", justification = "agent-lab local-git set")
prefix_rule(pattern = ["git", "show"], decision = "allow", justification = "agent-lab local-git set")
prefix_rule(pattern = ["git", "merge"], decision = "allow", justification = "agent-lab local-git set")
prefix_rule(pattern = ["git", "rebase"], decision = "allow", justification = "agent-lab local-git set")

# -- forbidden: remote integrity --
prefix_rule(pattern = ["git", "push"], decision = "forbidden", justification = "agent-lab: the human owns the remote/PR")
prefix_rule(pattern = ["git", "pull"], decision = "forbidden", justification = "agent-lab: the human owns the remote/PR")
# guard-enforced (remote ref, not a token prefix): git merge origin/*
# guard-enforced (remote ref, not a token prefix): git merge upstream/*
# guard-enforced (remote ref, not a token prefix): git rebase origin/*
# guard-enforced (remote ref, not a token prefix): git rebase upstream/*
prefix_rule(pattern = ["gh", "pr"], decision = "forbidden", justification = "agent-lab: the human owns the remote/PR")
prefix_rule(pattern = ["gh", "release"], decision = "forbidden", justification = "agent-lab: the human owns the remote/PR")
prefix_rule(pattern = ["git", "remote", "add"], decision = "forbidden", justification = "agent-lab: the human owns the remote/PR")
prefix_rule(pattern = ["git", "remote", "set-url"], decision = "forbidden", justification = "agent-lab: the human owns the remote/PR")
prefix_rule(pattern = ["git", "remote", "remove"], decision = "forbidden", justification = "agent-lab: the human owns the remote/PR")
prefix_rule(pattern = ["git", "remote", "rename"], decision = "forbidden", justification = "agent-lab: the human owns the remote/PR")
# <<< END GENERATED <<<
